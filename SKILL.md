---
name: handwriting-cutout
description: Extract handwritten signatures, digits, and scribbles from photos into transparent-background PNGs for PDF e-signing and stamping. Thresholds are estimated per image, so no per-image tuning is needed. 触发词：抠签名、抠字、把手写签名抠出来、做电子签、透明底签名、照片去白底、签名透明背景、把字从图片里扣出来、signature cutout.
---

# 手写签名抠图 → 透明底 PNG

## 何时用

用户给一张手机拍的手写签名 / 数字 / 字迹 / 印章照片，要求：抠出来、去背景、做电子签、
叠到 PDF 或 Word 上、要透明底 PNG。

## 怎么用（一条命令，不要试参数）

```bash
python scripts/cutout.py <图片...> [-o 输出目录] [--name 名称]
```

- 支持**多图批量**：多张图一次传入。
- **默认全自动**，无需指定任何阈值。不同笔色、纸张、光照、清晰度、正反相都能直接出。
- 输出：`_black` / `_blue` / `_red` / `_ink`（原图真实墨色）透明底 PNG，
  加 `_白底.png`（不认透明通道的工具用）和 `_预览.png`。
- 依赖：`pip install pillow numpy scipy`（scipy 可选，只影响连通域计算速度；
  没有 scipy 会自动退回纯 Python BFS，结果一致）。

## ⚠️ 默认值不要写死

用户会拿**各种不同的图**来抠，所以本脚本的设计前提是**参数不许固化**：

- 脚本里的 `LO_REL / HI_REL` 是**相对值**，作用在"已按本图自身动态范围归一化之后"，
  天然适配不同图片。改这两个数会破坏通用性。
- 需要微调时**用命令行开关**（见下表），不要改代码里的默认值。

| 开关 | 用途 |
|---|---|
| `--lo 0.16 --hi 0.55` | 手动绝对阈值，覆盖自动（只在自动失败时使用） |
| `--ink auto\|lum\|chroma` | 墨色提取方式；auto 会自动判断是否彩笔 |
| `--sharpen 140` | 锐化强度；默认按模糊程度自适应（糊→180，中→140，清晰→95） |
| `--scurve 2` | 边缘 S 曲线次数；`0` 更柔和，`3` 更硬 |
| `--close` | 桥接断笔（浅铅笔、糊照片断线时用） |
| `--min-comp 0.02` | 保留连通域的最小面积比（相对最大块） |
| `--pad 10` | 裁切留边（像素） |
| `--height 1600` | 输出高度（像素） |
| `--colors black,blue,red,ink,white` | 选择输出哪些颜色版本 |
| `--no-preview` | 不输出预览图 |
| `--debug` | 额外输出诊断图（直方图 + 阈值落点 + 结果缩略图） |

## 自动参数怎么算的（改之前先读懂）

1. **光照归一化**：`raw = 1 - gray / blur(gray, radius=长边/12)`。
   除以自身的大半径模糊 → 消掉阴影和打光不均。这一步是成败关键：
   **直接阈值/魔棒抠不出效果**，阴影会被当成墨迹，抠出来一圈灰雾。
2. **按本图动态范围归一化**：纸面 = `p50(raw)`，墨上限 = `p99.5(raw)`，映射到 0..1。
   这是"不固化"的核心——不管原图对比度强弱，归一化后形态一致。
3. **相对色阶 + 锐化 + S 曲线 ×2**：`a*a*(3-2a)` 让笔锋收利落、消除灰晕。
   少了 S 曲线，字看着像糊的。
4. **连通域过滤**：只丢"相对最大块 < 2%"的碎点，所以多笔画、多字符会**整体保留**。
5. **彩笔**：`auto` 下若墨迹区平均饱和度 > 0.25 → 改用"最深的单通道"提取，红/蓝/橙笔都稳。
   **深底浅字**（黑板）自动反相。

## 自检 / 回归测试

```bash
python scripts/gen_cases.py ./examples/generated        # 生成 6 种场景测试图
python scripts/cutout.py ./examples/generated/case_*.png -o ./examples/generated/out
```

覆盖：米色纸+旋转、蓝笔+强阴影、橙色+重度模糊小字、楷体多字（多连通域）、
深色底浅字（反相）、浅铅笔（低对比）。已验证 6 例一次通过，批量约 3.5 秒。

## 坑

- 源图越糊，成品边缘越"肉"，这是物理限制；按实际签名尺寸（3–5cm 高）看是正常的，
  不要为这个反复调参。
- 自动失败（抠没了 / 抠出一片黑）时才动 `--lo/--hi`，并加 `--debug` 看阈值落在哪。
- **不要一上来就跑参数扫描做对比图**——用户嫌慢。内部自检可以，但只交最终成品。
- 别把用户本人的真实签名传到公开仓库/公开链接里。演示一律用 `gen_cases.py` 生成的合成样例。

## 交付话术

给文件 + 一句插入方法（PDF 编辑器 → 插入图片 → 拖到位置 → 调到 3–5cm 高，透明底会自动透出下层文字），
并主动问"要不要我直接盖到 PDF 上"。
